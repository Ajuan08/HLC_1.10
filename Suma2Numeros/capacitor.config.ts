import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Suma2Numeros',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
